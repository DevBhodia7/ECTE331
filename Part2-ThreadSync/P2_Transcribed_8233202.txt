moving towards our project part 2 the same way we will open the hierarchy we will go under src open our package and this time it's part 2 threads and the file that will be running its problem to app .java run as java application we also will because it's a very short program and the iterations are set at default at 100 ,000 we don't really need to set the arguments but we can just verify that quickly yes such as 100 ,000 click run we'll just open the program in the background and yeah this is the console so i'll quickly go through the overview of the project this project is around thread synchronization communication the tasks that we had were we were given two threads thread a and thread b and the main issue was that some functions depend on values calculated by the other thread so each thread had three functions a1 a2 b a1 a1 a2 a3 and b1 b2 b3 and the issue was that the functions depended on the values to be calculated by the other thread before they can perform so for example for b2 to run a1 needed to be done before or for example if b3 needed to run a2 needed to be done before that so that was the issue but the most important dependency chain over here was a1 and b1 needs to be done before b2 then b2 before a2 and b3 before a3 because right now we can see that function b1 has started one of my previous iterations a1 had started and then b1 took place because function a1 and b1 are independent so either one can print first depending on the operating system scheduler and that is normal for threads for synchronization we'll just go over the process now where your b1 starts then we go to a1 then we go to b1 again and because a1 and b1 are done only then we are able to start with b2 and it's the same synchronization process start to end so this is the whole printed value a1 this is the final value that we print a1 b1 b2 a2 then b3 then a3 and then that's the whole thing done over here we can also see the number of iterations so we passed 100 iterations it was zero fail iterations and that's why we print the verification pass The reason why we're able to do this is because we use some morphs. And the reason why we're able to do this is because I use some morphs. And that is generally what's used to block a thread until the value it needs to be ready. This avoids the activating and also avoids the thread .sleep, for example, as that was one of the requirements. And yeah, that's pretty much it for part two.